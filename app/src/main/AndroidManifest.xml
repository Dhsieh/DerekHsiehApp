<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="derekhsieh.derekhsiehapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SET_DEBUG_APP" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name="login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="signUp.SignUpAcitivty"
            android:label="@string/title_activity_sign_up_acitivty"></activity>
        <activity
            android:name="mainPage.MainPageActivity"
            android:label="@string/title_activity_main_page"></activity>
        <activity
            android:name="friends.friendRequest.FriendRequestActivity"
            android:label="@string/title_activity_friend_request"></activity>
        <activity
            android:name="friends.friendList.FriendListActivity"
            android:label="@string/title_activity_friend_list"></activity>
        <activity
            android:name="friends.friendPage.FriendPageActivity"
            android:label="@string/title_activity_camera_activity"></activity>
        <activity
            android:name=".LoginActivity3"
            android:label="@string/title_activity_login3"></activity>
    </application>

</manifest>